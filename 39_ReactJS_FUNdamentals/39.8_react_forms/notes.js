/**
 * React forms - The React Way!
 * Forms naturally manage their own internal data
 * One source of truth - React
    * Need 'onChange' handler - updates state as user interacts with program
    * handleSubmit, add values from inputs to a form object
 * 
 * Labels in React
    * Add labels w/ label tags
    * htmlFor is React's for in html we need to use 
    * 
 * Forms w/ more than one input
    * Scale your handleChange useState function
    * onSubmit child to parent (pass it from parent)  
    * 
 * Use Formik for React form validation
 * Material-UI has premade form components  
 */