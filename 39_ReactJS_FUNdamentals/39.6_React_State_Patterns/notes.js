/**
 * React State Patterns
 * cannot put usestate in a nested conditional loop or function
 * 
 * keys
    * usually you will have a unique identifier from db
    * try not to use idx from map function as key
    * 
 * 
 * minimize states
 */